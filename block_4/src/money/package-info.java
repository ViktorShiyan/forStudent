/**
 * Создать класс Money с полями валюта и сумма, конструктором с
 * параметрами. Создать класс Test, содержащий метод static Money sum(Money m1,
 * Money m2), складывающий суммы с учетом валюты (если валюта не совпадает,
 * приводим к одной и складываем). Вызывать sum в main.
 */
package money;